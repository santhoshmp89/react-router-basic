import React, {Component, Fragment} from 'react';
import Menu from '../Menu';

const About = (props) => {
    return(
        <Fragment>
            <Menu />
            <h1>About</h1>
        </Fragment>
    )
}

export default About;
