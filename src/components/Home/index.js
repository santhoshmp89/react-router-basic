import React, {Component, Fragment} from 'react';
import Menu from '../Menu';

const Home = (props) => {
    return(
        <Fragment>
            <Menu />
            <h1>Home</h1>
        </Fragment>
    )
}

export default Home;
